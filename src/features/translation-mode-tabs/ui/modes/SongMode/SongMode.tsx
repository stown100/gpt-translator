
import styles from './SongMode.module.css';

export const SongMode = () => {
  return (
    <div className={styles.placeholderWrapper}>
      <p className={styles.placeholder}>Song mode — in development.</p>
    </div>
  );
};
