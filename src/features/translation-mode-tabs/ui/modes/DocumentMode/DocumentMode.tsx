import styles from './DocumentMode.module.css';

export const DocumentMode = () => {
  return (
    <div className={styles.placeholderWrapper}>
      <p className={styles.placeholder}>Document mode — in development.</p>
    </div>
  );
};
