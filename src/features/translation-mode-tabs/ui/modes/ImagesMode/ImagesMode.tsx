import styles from './ImagesMode.module.css';

export const ImagesMode = () => {
  return (
    <div className={styles.placeholderWrapper}>
      <p className={styles.placeholder}>Images mode — in development.</p>
    </div>
  );
};
